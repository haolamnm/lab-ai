# Comma-separated list of browser origins allowed to call this backend. The Vite
# dev server runs on 5173 by default; add your deployed frontend origin here in
# production. Unset falls back to the localhost dev origins below.
ROUTELAB_CORS_ORIGINS=http://localhost:5173,http://127.0.0.1:5173
