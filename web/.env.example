# Point the frontend at the Python planning backend (server/). When set, the
# Run button sends each pane to POST $VITE_API_URL/plan instead of computing
# in the browser. Leave unset to run the built-in TypeScript planner. The
# sample graph always runs locally regardless of this setting.
VITE_API_URL=http://localhost:8000
